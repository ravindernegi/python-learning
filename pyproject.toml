[project]
name = "python-code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ravinder Singh", email = "r.singh.negi85@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "pandas>=3.0.5",
    "requests>=2.34.2",
    "uvicorn>=0.52.1",
]

[project.scripts]
python-code = "python_code.app:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
